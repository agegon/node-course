<nav>
    <div class="nav-wrapper blue blue-darken-4">
      <a href="/" class="brand-logo">Courses</a>
      <ul id="nav-mobile" class="right hide-on-med-and-down">
        {{#if isHome }}
          <li class="active">
        {{else}}
          <li>
        {{/if}}
          <a href="/">Главная</a>
        </li>
        {{#if isCourses }}
          <li class="active">
        {{else}}
          <li>
        {{/if}}
          <a href="/courses">Курсы</a>
        </li>
        {{#if isAdd }}
          <li class="active">
        {{else}}
          <li>
        {{/if}}
          <a href="/courses/add">Добавить курс</a>
        </li>
      </ul>
    </div>
  </nav>
